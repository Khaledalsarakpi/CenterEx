CREATE TABLE traineetb (id INTEGER NOT NULL,name TEXT NOT NULL DEFAULT 'Unknow',address	TEXT NOT NULL DEFAULT 'Unknow',workplace TEXT NOT NULL DEFAULT 'Unknow',Adjectivejop	TEXT NOT NULL DEFAULT 'unknown',IdCourse INTEGER,qualification	TEXT NOT NULL DEFAULT 'unknown',phoneNumber TEXT NOT NULL DEFAULT 'unknown',idCard INTEGER NOT NULL UNIQUE,FOREIGN KEY(IdCourse) REFERENCES Coursetb (id),PRIMARY KEY(id AUTOINCREMENT));
CREATE TABLE Trainertb (id INTEGER NOT NULL,name TEXT NOT NULL,idcard	INTEGER NOT NULL UNIQUE,address	TEXT NOT NULL,qualification TEXT NOT NULL,phoneNumber TEXT NOT NULL,workplace TEXT NOT NULL,PRIMARY KEY(id));
CREATE TABLE Coursetb (id INTEGER NOT NULL,name TEXT NOT NULL,dateStart TEXT NOT NULL,hourseOfSet	INTEGER NOT NULL,state TEXT NOT NULL,idPlace INTEGER NOT NULL,idTrainer INTEGER NOT NULL,FOREIGN KEY(idTrainer) REFERENCES Trainertb(id),FOREIGN KEY(idPlace) REFERENCES Place(id),PRIMARY KEY(id AUTOINCREMENT));
CREATE TABLE degresstb (id INTEGER NOT NULL,Dgpresence	INTEGER NOT NULL,DgActivity	INTEGER NOT NULL,Dgtest INTEGER NOT NULL,idTrainee INTEGER,idCourse	INTEGER,FOREIGN	 KEY(idCourse) REFERENCES Coursetb(id),FOREIGN KEY(idTrainee) REFERENCES traineetb(id),PRIMARY KEY(id AUTOINCREMENT));
CREATE TABLE TrPlace (id INTEGER NOT NULL,name TEXT NOT NULL,PRIMARY KEY(id));